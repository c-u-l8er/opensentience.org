defmodule OpenSentience do
  @moduledoc """
  OpenSentience runtime — OS-008 Agent Harness Protocol.

  The harness is the enforcement runtime that sits above agents and below humans.
  It orchestrates [&] pipelines, enforces governance contracts, gates execution
  on epistemic confidence, and ensures no agent skips retrieval, fabricates
  provenance, or acts beyond its coverage.

  See `OpenSentience.Harness` for the public API.
  """
end
